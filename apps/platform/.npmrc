# React 19 peer dep compatibility
# Several packages (react-day-picker, recharts, etc.) have not yet updated
# their peerDependencies to include React 19. This flag lets npm install
# proceed without errors until the ecosystem catches up.
legacy-peer-deps=true
