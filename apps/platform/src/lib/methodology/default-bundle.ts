// Canonical source moved to prisma/methodology/default-bundle.ts so the seed
// script can run in production without requiring the src/ tree at runtime.
export { DEFAULT_METHODOLOGY_BUNDLE } from "../../../prisma/methodology/default-bundle";
